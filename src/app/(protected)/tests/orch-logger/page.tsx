import LoggerClient from "@/app/(protected)/system-b/browse-cert-specs-V4/loggerClient";

export default function OrchLoggerPage() {
  return (
    <div style={{ padding: 16 }}>
      <LoggerClient tabId={null} />
    </div>
  );
}